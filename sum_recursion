#include<iostream>
using namespace std;

int sum (int n)
{
    if (n == 0)
    {
       return 0;
    }
    else
    {
      return (n % 10 + sum (n / 10));
    }
}

int main() 
{
 int num;
 cout<<"enter tne number\n";
 cin>>num;
 cout<<"sum of digits is \n"<<sum(num);
}

